name: Task 1 CI/CD

on:
  push:
    branches:
      - main

jobs:
  install:
    uses: ./.github/workflows/install.yml

  test:
    needs: install
    uses: ./.github/workflows/test.yml

  build:
    needs: test
    uses: ./.github/workflows/build.yml